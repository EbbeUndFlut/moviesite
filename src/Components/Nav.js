import Searchbar from "./Searchbar"

const Nav = () => {
    return (
        <header>
            <h1>
                <span>.</span>MOV
            </h1>
            <Searchbar />
        </header>
    )
}

export default Nav
