import Searchbar from "./Searchbar"

const Nav = () => {
    return (
        <header>

            <a href="/"><span>.</span>MOV</a>
            <Searchbar />
        </header>
    )
}

export default Nav
